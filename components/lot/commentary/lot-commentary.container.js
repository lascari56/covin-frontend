import React from 'react';

import LotCommentaryView from "./lot-commentary.view"

const LotCommentaryContainer = ({...props}) => {
  return (
    <LotCommentaryView {...props} />
  );
}

export default LotCommentaryContainer;
